import { NumeroFormatadoPipePipe } from './numero-formatado-pipe.pipe';

describe('NumeroFormatadoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroFormatadoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
